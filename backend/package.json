{
  "name": "credibility-validator-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start:selfhost": "tsx src/selfhost.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "typecheck:selfhost": "tsc --noEmit -p tsconfig.node.json"
  },
  "dependencies": {
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.1",
    "@cloudflare/workers-types": "^4.20260426.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.86.0"
  }
}
